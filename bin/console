#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "filter_param"
require "irb"
require "pp"

Dir[File.expand_path("../spec/support/*.rb", __dir__)].sort.each { |f| require f }

IRB.start(__FILE__)
